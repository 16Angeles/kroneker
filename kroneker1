import numpy as np
a = [[1, 2],
     [3, 4]] #Заполнить первый элемент тут
b = [[1, 0, 2],
     [0, 2, 3]] #Второй тут
c = np.kron(a, b)
print(c)


def PA():
    if 100 > 1:
        return 'Паша Гилев круче всех!'

print(PA())
